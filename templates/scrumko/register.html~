{% extends 'scrumko/main.html' %}

{% block body_block %}
        <h1>Dodajanje uporabnikov</h1>

        {% if registered %}
       	Uporabnik je bil <strong>uspešno  </strong>dodan.<br/>
        <a href="/scrumko/register/">Dodaj še enega uporabnika.</a><br/>
        	
        
	{% else if %}
        <form id="user_form" method="post" action="/scrumko/register/"
                enctype="multipart/form-data">

            {% csrf_token %}
	    {% for hidden in user_form.hidden_fields %}
		{{ hidden }}
	    {% endfor %}
	    {% for field in user_form.visible_fields %}
					<div>
						<div class="fieldwrapper">
							{{ field.label_tag }}
						</div>
						
						<div class="fieldwrapper">
							{{ field }}
						</div>
						
						<div class="errorwrapper">
							{{ field.errors.as_text }}
							
						</div>
					</div>
					<br/>
					<br/>
	    {% endfor %}
	     {% for hidden in profile_form.hidden_fields %}
		{{ hidden }}
	    {% endfor %}
	    {% for field in profile_form.visible_fields %}
					<div>
						<div class="fieldwrapper">
							{{ field.label_tag }}
						</div>
						
						<div class="fieldwrapper">
							{{ field }}
						</div>
						
						<div class="errorwrapper">
							{{ field.errors.as_text }}
							
						</div>
					</div>
					<br/>
					<br/>
	    {% endfor %}
 
         
			
            <!-- Provide a button to click to submit the form. -->
            <input type="submit" name="submit" value="Dodaj" />
        </form>
       {% endif %}
{% endblock %}
